<img width="1079" height="729" alt="image" src="https://github.com/user-attachments/assets/fbfe44fe-03c0-42c3-bfdf-f4af5d1259fc" />
и этим всё сказано 

автоответчик + реакт. агент для телеграмма (я хочу всех игнорить)

Как начать:

1. Копировать репозиторий
2. Создать файл: TGA.UI\appsettings.json, подставить свои ApiId и ApiHash, остальное советую не менять...

```
{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  },
  
  "AllowedHosts": "*",
  
  "Telegram": {
    "ApiId": "ПОСМОТРЕТЬ НА my.telegram.org",
    "ApiHash": "ПОСМОТРЕТЬ НА my.telegram.org"
  },
  
  "ConnectionStrings": { 
    "Default": "Data Source=telegram_assistant.db"
  },
  
  "HistoryLoad": { 
    "DefaultMessagesPerDialog": 20,
    "MaxMessagesPerDialog": 100
  },
}

```
3. ВКЛЮЧИТЬ ХОРОШИЙ ВПН (это очень сильно влияет на вход) (работал с v2RayTun, но тоже через раз)
4. запустить из UI -> откроется на порту http://localhost:5213 (см. консоль)
5. перейти в `/login` "Вход" и начать auth flow
6. возможно придётся долго подождать (логи можно посмотреть на Главной)
7. проходим аутентификацию и ОБЯЗАТЕЛЬНО нажимаем "загрузить диалоги"

Фактически клиент создан, но что бы настроить агента:

1. переходим в `/llm-settings` Агент - заполняем форму и проверяем работоспособность (произвольно)
2. открываем `/contacts` Контакты и начинаем редактирование любого интересующего контакта (правый столбец с карандашиком)
3. заполняем форму (произвольно, согласно подсказкам) и включаем автоответчик
4. просим выбранный контакт написать пару сообщений, открываем Главную и ждём 7 сек. после последнего пришедшего сообщения
5. произойдёт Триаж - он решит ответить или проигнорировать
6. если решение ответить, то агент ответит.


## Путь сообщения

Telegram Message 

    ↓

1 BUFFERING & DEBOUNCING

  AutoReplyDebounceService

  - Накапливает сообщения до DebounceSeconds
  - Может расширить ожидание MaxWaitExtensions раз

    ↓

2 TRIAGE (Классификация) 

  TriageService → LLM Call #1

  - Temperature 0.1 (детерминированно)
  - КОНТЕКСТ: профиль контакта + последние N сообщений + новые сообщения
  - РЕШЕНИЕ: Reply / Wait / Skip

    ↓

3 AGENT EXECUTION (Re Act Loop) 

  AgentService → LLM Call #2... #N

  • КОНТЕКСТ (многослойный):

  - Системный промпт
  - История (HistoryContextSize)
  - Few-shot примеры (если Ghost mode)
  - Триггер-сообщения
  - Результаты инструментов (итеративно)

    ↓

4 PERSISTENCE

  AgentRunStorageService → Database

  - Сохранение состояния между уточнениями
  - Дедупликация импортированных сообщений

## Что конкретно попадает в LLM Запросы

LLM Request #1: TRIAGE (легкий запрос)

System: "Ты модуль триажа... Реши: reply / wait / skip?"

User Prompt:
```
├─ Контакт: DisplayName, Notes, CommunicationStyle, AutoReplyInstructions
├─ Контекст: Последние HistoryContextSize сообщений
└─ Триггер: Новые unbuffered сообщения
```
LLM Request #2+: AGENT EXECUTION (тяжелые запросы)

1. Если Ghost Mode (~300-400 токов):

- Инструкция имитировать стиль пользователя
- Few-shot примеры (GhostStyleExampleCount ~ 12 собственных сообщений пользователя)
- Заметки о собеседнике (profile.Notes)
- Профиль поведения (profile.BehaviorProfile) 
- Доп. инструкции (profile.AutoReplyInstructions)

2. Если Assistant Mode (~150-200 токов):

- Инструкция быть помощником
- Те же metadata + инструкции

#### Общее для всех режимов 

- ЗАПРЕТ: "Никогда не придумывай факты, даты, события"
- ОБЯЗАННОСТЬ: Использовать только результаты инструментов

User Prompt (каждая итерация):
```
[Первый запрос]:
├─ "Последние сообщения переписки:" 
│  └─ [История: время, от кого, текст] × HistoryContextSize
├─ "Новые сообщения, на которые нужно отреагировать:"
│  └─ [Триггер сообщения] × N
│ 
[Последующие итерации]:
├─ Assistant Role: [предыдущий ответ + tool_calls]
├─ Tool Role: [результаты выполнения инструментов]
└─ User Role: [новая информация, если уточнения]
```

## TODO 

1. Добавить возможность выбора чата, в который будет приходить команда, например:

- "/stop" 
- "/start" 
- "/responsne `username` " ← парс по контактам или базе знаний
- "/search" ← простой поиск 
- "/research" ← глубокий поиск

2. Добавить токен мониторинг -> логировать token usage из LLM response, установить мониторинг

3. Для чатов с большой историей (только для зумерского использования)  Consider semantic search или summarization для больших истории

4. Для Ghost Mode добавить настройку Few-shot (реализовать полноценный подбор фраз)

- Фиксированный набор из 5–7 реальных диалогов, которые максимально ярко демонстрируют стиль в разных ситуациях (просьба, жалоба, шутка, согласие, отказ, совет). Эти примеры вставляются в системный промпт всегда. 

- Динамический подбор дополнительных 3–5 примеров, релевантных по смыслу к текущему запросу (через векторный поиск по эмбеддингам сообщений). Это обеспечивает контекстную релевантность.

5. Ну сильно в будущем token streaming или compression для последующих итераций ReAct (сейчас: каждое уточнение = полная переотправка истории)

6. При вызове tool :  remote search синхронный вызов, может добавить 2-5 сек на запрос, т.ч. надо timeout, graceful degradation

7. Так-же для tool добавить ранжирование результатов (например топ-5) т.к. нет структурирования по релевантности и LLM получает результаты как LlmChatMessage с Role=Tool и LLM может "утонуть"

8. Важное для хорошей интерактивности: Debounce logic может потерять срочные сообщения, добавить Keyword detection (urgency markers) для ускорения триажа, например "СРОЧНО ПОЗВОНИ"

#### Удобства: 

9. добавить подключение к гугл календарю 

10. Блек лист - установить ограничения на поиск по сообщениям (сообщения от этого пользователя не идут в учёт)

  ↓

  Из вышесказанного вытекает контроль приватности диалогов 

11. Дополнить PathHelp для ввода пути в  парсер (export)

12. подписаться на “печатает...” 